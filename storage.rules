rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true; // Public read for demo; tighten in production
      allow write: if request.auth != null; // Functions write with admin SDK
    }
  }
}

